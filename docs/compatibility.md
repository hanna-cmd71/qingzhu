# 兼容规则与维护沿革

适用于青竹剑阵 1.0 的维护。以下保留历史要求全文以便核对旧引擎；其中“新局”指当时批次，当前默认以根 AGENTS.md 与各源码常量为准。历史批次不是游戏当前版本。

1.0 发布修订优先：隔离满 12 份不自动淘汰；清理须比较四类存储的确认前快照。经济 4 和遭遇 8＋平衡 4 的在途局未来新节点按当前生成器处理，已保存整备／幻境保留字段语义。发布日志仅为“1.0正式发布”，具体修复见验收报告。

---

# 青竹剑阵项目约定

适用于本目录及所有子目录。以下维护要求来自用户的明确指示，后续工作必须遵守。

## 玩法、说明与更新日志同步

- 任何新增、调整或删除的玩法内容，都必须在同一次工作中同步更新根目录的 `青竹剑阵_游玩说明.md`，不能只改游戏后留待下次补文档。
- 同步范围包括：操作和界面提示、武器与召唤物、技能和资源、成长数值、流派与装备、敌人和关卡目标、模式、解锁条件、存档与玩家操作流程。
- 说明应描述当前实际实现的规则；核对名称、数字、触发条件、操作入口和限制，删除或修正失效说明。尤其不要混淆强化种类数与召唤物数量、灵力与雷源、取得器物与可使用状态。
- 每次更新必须同步记录到 `青竹剑阵_更新日志.md`，面向玩家说明改了什么、玩法或体验有什么变化，以及对应说明是否已更新。不要把计划或未完成内容写成已经实现。
- 同一版本内继续维护时，在该版本下追加实际修订；新版本按用户指定的版本号建立新条目，版本倒序排列。不要自行把当前版本宣布为正式版。

## 当前版本与交付

- 当前游戏显示版本为 **0.2beta**；统一来源是 `game/gameplay/version.js` 的 `GAME_VERSION`。
- 当前作品篇名为 **虚天殿篇**，统一来源为同文件的 `GAME_ARC`；网页标题使用 `GAME_TITLE`。篇名调整不改写剧情中的地理区域名称。
- 游戏界面、HTML 标题、游玩说明、更新日志、README 及当前交付文档的游戏版本标记须一致。方案文件本身的修订号与游戏版本号分开，不要全局替换依赖库版本。
- 游戏版本与存档格式版本分开管理。现有存档格式为数字 `1`，本地存档键为 `fanren-qingzhu-v1`；仅调整游戏版本号时不得修改它们或清空玩家进度。
- 修改可执行游戏内容或版本展示后，重新生成根目录的 `凡人修仙传_青竹剑阵.html`；源码与文档更新后同步 `青竹剑阵_源码.zip` 内的对应文件，包括本约定和更新日志。
- 保持交付文件可离线游玩。按改动范围验证实际行为；涉及玩法时核对说明及日志后再报告完成。

## 内容边界

主篇为乱星海炼剑后至虚天殿。第六章名称为「虚天夺鼎」，对应出鼎冰焰退避与玄骨终局；不得混入后期小极宫人物。按原著处理关键事件、能力取得顺序和人物结局。波次、数值与战术是游戏化规则，不能冒称原著事实；未实现或未核实内容不得写进已实现清单。

## 已确认的交互与素材要求

- 首页保留完整制作署名：`制作人：bilibili@卡布奇诺ultra`。
- 噬金虫使用金色友方素材，与敌方甲虫区分。
- 所有可能覆盖存档或造成未保存进度回退的游戏内入口，必须先说明具体影响并确认；确认弹窗暂停战斗，取消不能修改当前局或存档。
- 新局、导入、备份恢复等覆盖动作必须保留可恢复的旧记录；读写失败不得静默清空或覆盖。多窗口冲突停止旧页写入。
- 游戏版本与起手规则版本也分开：新开局使用秘境规则 3；规则 1／2 的旧存档继续使用各自原引擎，缺少规则标记按规则 1 恢复，不能半途转换路线或强度。
- 游戏内帮助由根目录说明及更新日志生成，打包时运行文档嵌入，避免游戏内外说明不一致。

- 秘境节点、路线、事件链、核心、药园和过场数据在 expedition-data.js / expedition.js。规则 3 允许战场快照；经验与研究结算必须有唯一凭据。普通路线不得改写关键原著因果。

- 新整备的机缘与核心须保持出发前可改选、提交时一次结算；不要把暂选当作已发放奖励。旧版已经领取的记录不得凭空回溯或重复发奖。洞府洗点全额返还且保留当前续玩出发配置，确认前列明影响、覆盖前备份。

- 洞府修持有独立的 metaRulesVersion：新版为 3，只启用起手对应分支；规则2节点定义完整保留；没有此标记的旧局按 1 恢复旧准备数值。旧账号已购修持须确认后全額返还并切换新树，不能默默用同一 ID 套用不同效果。所有节点文案必须对应实际战斗效果。

- 备份覆盖须保留可中断恢复的前态；不能因主键保存失败丢失旧备份。隔离原文保留多份且可从设置导出。丢弃损坏的续玩以恢复长期进度必须先确认。

- 战斗平衡与秘境路线、洞府修持、存档格式独立：新局 `options.balanceVersion` 为 3；缺标记的旧秘境局恢复为 1，保留固定符阵引信、3 秒护身闪避、原雷源预算与物资概率。不得在续玩时静默升级。

- 敌人与物资内容单独保存：新局 `options.contentVersion` 为 3；规则3在28种章节敌人中等待预算生成，保持规则2的物资权重。内容规则2继续保留原28种池与原逐帧抽样；缺标记旧局按1恢复24种池及原护身物资加权，不半途更换刷新逻辑。Boss 死亡特效不得重复发奖或提前结束玄骨反噬。

- 鼠标集火分配单独保存：新局 `options.focusVersion` 为 3；缺标记旧局按 1 恢复原分组三目标优先级。新集火仅影响新出击剑的首目标，不重置在途飞剑、冷却、穿透或射程；护阵需明确提示松开集火恢复分守。

- 触屏点按或拖动战场设置瞄准点，松手保留；右上角可以解除集火。暂停、悟道、整备、帮助及确认窗口清除瞄准输入。沿用本局原集火规则，不改变射程、在途飞剑或伤害。
- 新键位只能在设置中完成绑定；Esc 与离开设置必须取消重绑。已有不可用键位提示修正，不静默改写旧记录。
- 取景须同时照顾实际顶部状态、底栏与桌面侧栏，绘制与输入使用同一相机。游戏内说明的目录、搜索与阅读位置只作用于当前页面，内容仍由根目录文档生成。

- 新局 `options.inputVersion` 为 2，支持最多 120 毫秒、最多一次的闪避缓冲；缺标记旧局按 1 恢复即时输入。缓冲不序列化，暂停、悟道、整备、帮助、确认、设置、失焦与离开战斗时清除；不改变本局闪避数值。
- 异常恢复日志与孤立备份须展示可读进度并停止覆盖性写入；确认处理前保留主档、旧备份与日志原文，写入失败或并发变更时停止。受保护旧记录可多份导出及确认恢复，不随常规新局轮换备份而丢失。
- 分步操作练习只在试剑台运行，按实际输入推进；结束返回出发配置，不写正式战场、不发心得或研究奖励。新增声音与视觉反馈不消费战斗随机数。

- 新局 `options.growthVersion` 为4；缺标记旧局按1恢复。新候选按实际配置筛选，保留跨路入口与混合收益；首次保底状态、已生成候选不得因读档重抽。基础／表现经验的分配、拾取和结算分别保存，每节点表现上限为基础25%，不抵扣保底；奖励凭据按本局、无尽轮次和节点区分。
- 新局 `options.encounterVersion` 为7；缺标记旧局按1恢复。四类目标节点130秒追兵后进入清场，只停止非任务新增，保留完成目标所需有限生成；非任务、非首领敌人（含周期精英）软上限35／50／65。进入清场后保存一次；恢复不能重新计时或突发补兵。关键剧情目标不因遭遇规则转换。

- 第十轮 M15—M17：平衡3仅新局启用雷法基础22、符伤每剑8%（12至72剑）、驻定0.3秒衔接、每次成功闪避一次半伤反击与3回盾；平衡1／2原实现保留。符圈体积相交用于触发、爆炸及反馈，衍生伤害只从原始快照派生一次。
- 修持3保留36个ID与价格，循息引流改为合格休整+5雷源、轻墨减少布符0.15秒（最低0.1、合符配对0.65秒）、群息按3秒内虫群协击每8只最多回3血。旧修持1／2确认全额返还后切树，出发时旧配置与未决符不转换。
- 新局 `options.economyVersion` 为4，缺标记按1恢复。核心当前有效且去重，候选只生成一次，暂选不发奖；第四章起至终战前的整备可购买悟道重选，全局最多两次80／120灵石，凭据持久化，无尽换轮不重置。旧经济3保留原重选与终战整理；旧经济2继续原商店入口；旧经济1—3不开放补养。

- 第十轮 M18—M19：成长3只前移900基础经验，主篇保底11960不变；旧成长1／2保留原分布。遭遇3取宝3×10秒、幻境首次7秒，旧遭遇1／2保留42秒／首次10秒；普通追兵130秒、冰焰40秒、玄骨反噬13秒不变。
- 新局 `options.storyVersion` 为3；规则2保留原触发与章节事件抽池，缺标记按1恢复原触发与事件抽池。新增第二／第五章三幕过场；24项机缘只改章节筛选与文本，不改奖励语义，持有剧情器物与战场协战状态分别处理。过场说明置于角色对白之外，跳过不发重复奖励。章节界面名称统一用 `chapter-names.js`，兼容旧第四章成就名称。
- 72剑演示是约2.4秒纯表现，不改变真实飞剑、战斗随机数、时间、镜头或奖励；取宝／悟道期间达成可待回到战斗再播。首领标识与韩立新过场头像不更换战斗碰撞规则或友方素材。

- 第十轮 M20：设备设置保留左／右摇杆、两档大小、左／右技能；默认原布局，设置期间暂停清输入。集火3才支持默认关闭的触屏锁敌；旧1／2不在暂停时升级。拖动、目标失效或越界按约定解除，正常续玩经暂停清锁；不改真实飞剑、射程、冷却或伤害。快照只存合法目标ID。touchLockUsed一旦开启保持，成绩按是否曾开启分组。
- 第十轮 M21：新局 `options.recordVersion` 为1，缺标记旧局按0不补造排名。recordState保存计时起点及最近完成圈的时刻／当时锁敌标记；记录实际完成圈，不用失败时刻代替完成时刻。主篇／定种完整22节点胜局才排最佳时间，无尽按完成圈次；模式、难度、流派、规则和锁敌使用分组。本地records为可选schema1，不改存档格式1。结算与圈次凭据不截断遗忘，重复结算不重复奖励。成绩损坏须说明并确认隔离，保留有效进度与原文。
- 第十轮 M22：原PNG继续保留，交付编码及哈希在public/assets/encoded/manifest.json。嵌入前核验原图与副本，原图变更须重新编码与验证；不能静默沿用旧副本。常规WebP离线HTML和同功能PNG资源兼容版都要更新；兼容版构建后恢复默认嵌入资源。浏览器半透明预乘通道的取整差不冒称逐像素全同；真机兼容及单手手感仍待验证。

- 第十一轮 QA A0—A5：异常记录及恢复包先只读预览、显式确认再采用；合法成绩凭据全集保留，坏／冲突成绩显式隔离，不造凭据。恢复前保护完整前态和导入原文，并比对主档／备份／日志／隔离资料，过时预览不可覆盖。无尽新局以wins>0准入，旧无尽checkpoint继续可玩。相机缩放与绘制、输入逆变换同源，非位置型首领不得生成NaN方向标记；关键面板正文与固定操作区分别占位。

- 第十一轮 QA B1—B4：成绩先比较再写入，成功保存后才显示已保存；无尽仍用完成圈时刻与当时锁敌分组，复用真实history，不扩成绩schema。可加属性来源对账与倍率／条件分开。新叙事3保存cinematicHistory跨圈已看记录，本圈cinematicSeen须为其子集；自动跳过仅设置开启且本局已经看过，仍走advanceCinematic正式推进，旧叙事1／2与缺标记旧局保持原触发。普通遭遇本地化仅显示，不改路线ID或选择。声音总上限12，优先保障受击和机制，静音／暂停立即停止，不消费战斗随机数。

- 第十一轮 QA C1—C5：新成长4第五章基础为1950/2050/1900/400，第六章400/200/250/150，主篇11960不变；限时目标表现与击杀表现合计每节点最多25%，goalXP独立记录且一次结算。旧成长1/2/3完整保留。新遭遇4用独立种子域一次生成并严格保存routePlan，仍消耗冻结旧生成器原抽样以保持机缘随机链；旧1/2/3缺plan沿冻结生成器，新4缺plan拒绝恢复。4显式继承遭遇3的30秒取宝/首次7秒幻境及130秒追兵，不改内容3。新经济3第四章起至终战前每次整备可购重选，仍2次80/120，当前圈付款节点须已完成；主篇/定种终战新结局整理，无尽及旧经济1/2的待提交机缘保留。平衡/修持仍3；符阵增加真实输入的第六步练习，不发正式奖励，声音/显示不消费战斗随机数。

- 三批整体校对：战斗宽度≤420隐藏保存图标，≤360省去顶部头像，为完整生命数值留宽；保存摘要仍从暂停进入。不得仅验证元素外框可见而忽略内部数字裁切，首页既有滚动布局保持。

- 第十二轮 QA 第二步 B1—B5：玩家提示由当前局只读生成，不改奖励与计时。洞府从出发页进入按暂选起手预览，返回保留配置，已有checkpoint出发配置不随之转换。成绩当前/历史规则筛选只读浏览，不混组、不删旧成绩；当前规则标签引用各规则版本来源。神雷雷源预算与实际可施放分开，休整到账先列基础最多15再列修持余量，受容量限制且不重复结算。已看开关可作用当前及恢复的叙事3局，历史仍限同一局；器物持有与战场配置分开。

- 第十二轮 QA 第三步：符阵练习落点只在practice入口校正且保留原随机抽样；正式生成器不改。护身“待化解”只读本局真实窗口，空闪不发反击或回盾；御剑进度仍按单个目标，虫群扩大范围不得覆盖玩家明确集火或K07/K08策略。未采用的隔离实验候选不作为新局默认或已上线规则，实际采用项须以本步执行报告与最终源码为准。

- 第十二轮 C5：经济4完整继承经济3的机缘/核心、终战整理及重选两次80/120；新增补养只在4:0—4:3、5:0/5:1，每节点一次、全局三次且无尽不重置，每次固定80灵石，回血最多25%最大生命或补盾最多12至常驻上限+12，不补雷源、满状态不收费。5:2禁入。即时恢复与后续暂选/换章覆盖须说清，付费暂选失效需提示并可改选；独立交易类型/节点凭据必须持久化，旧经济1—3不加入口，不采纳实验价格配置。

- 等待关卡追兵修订：新遭遇5继承4的完整routePlan与目标计时，旧1—4续玩原样。第三章起普通节点／虫潮预算+35%、开场6点、周期精英首次22秒；三处机关取宝／幻境／冰焰加入本章有限追兵，上限12／18／24，首次18秒周期精英且同屏最多一名，取宝／幻境65秒、冰焰40秒停止非任务新增。沿用已有waveTime／spawnBudget／directorState／nextElite保存，不因恢复重新计时或突发补兵；原目标、经验上限、雷源、关键剧情及实体首领保持。

- 三处机关加密：新遭遇6只加强取宝／幻境／冰焰，完整继承5的其他节奏、4的routePlan及原目标计时；旧1—5续玩保持。机关预算每秒6.8／6.0／5.6点、开场10点，精英首次10秒／间隔20秒、同屏最多两名，难度活敌上限18／26／34。旧5配置完整保留；停止增援时点仍65／65／40秒，怪物血伤、物资、雷源、经验账本与成绩格式不变。

- 2026-09-14三步整改第一步：新遭遇7完整继承6追兵及4的routePlan，仅幻境强制返回增加0.45秒独立落点保护，objective.returnProtectedUntil随快照保留；真假门原计时保持，保护不触发护身反击/回盾，旧1—6原样恢复、成绩按遭遇分组。恢复导入重试只认可本事务精确写入状态，四类存储外部变化仍阻止覆盖，保留前态及导入原文。练习切步清实际dash与移动/缓冲，正式暂停语义不改。

- 2026-09-14三步整改第二步：图鉴分账号历史历经、本局真实storyInventory及实际配置，不按章节补发本局器物。百刃齐发只显示既有stats.damage继承，成型候选给实际收益，不含群锋与条件增伤，不改弩伤。空爆教学按已保存逐枚主符记录仅连续第三枚纯落空触发，命中/阻挡中断，复燃不混入；不新增存档格式。

- 2026-09-14三步整改第三步：新局独立segmentVersion1、无尽challengeVersion1，缺失均按0；recordVersion1、存档format1/key与records schema1、原十项RULE_KEYS保持。章末真实净时间严格验证，旧不倒推。挑战只限普通节点神雷或药囊，关键首领与三机关豁免，完整序列保存；分组过滤none但保留受限圈号，下一未完成圈不改已完成组。
- 对照体验studyVersion1只用固定种子、标准难度、空修持的雷法/符阵真实前两章；所有正式持久化/结算入口隔离，观察中设置只读。观察包单独导出/严格恢复，不写正式checkpoint、成绩或奖励，不替换正式pending结果；正式在场局暂停保留，结束观察返回。真人理解与无尽三圈意愿仍待验证。
- 双靶教学仅试剑台step2固定A/B并认新出击明确选B；护身可选挡招只认真实窗口并回3盾一次，重试/结束可达。摇杆与只读提示不得消费战斗随机数。
- 2026-09-14第二轮十玩家测试整改第一批：新遭遇8完整继承7（含幻境0.45秒落点保护、6的机关追兵配置与4的routePlan），不改刷怪速率、活敌上限、精英间隔或目标计时；只在取宝／幻境65秒、冰焰40秒增援停止的第一帧保存一次（objective.clearingSaved随快照保留，读档不重计时、不补增援、不重复保存），活敌封顶期间nextElite按半个间隔持续后移、解限不同帧补精英。旧1—7原样恢复，成绩按遭遇8分组。serialize()的统计字段与待保存记录解引用冻结；隔离原文上限12份、按原文去重、设置内确认清理；无尽已完成圈挑战选择须与本机同一局成绩一致；练习第五步只认真实松手。
- 2026-09-14第二轮十玩家测试整改第二批：新局 `options.balanceVersion` 为4、`growthVersion` 为5、无尽新局 `challengeVersion` 为2（其他模式仍0），遭遇8在平衡4下扩展数值（`extendedEncounter`）。所有本批数值只经 `balance.js` 的 `BALANCE4`／`director-rules.js` 的 `ENCOUNTER8`／`growth-rules.js` 的 `GROWTH5`／`experience-rules.js` 的 `CHALLENGE2` 表与门控函数读取（`starterExtras`、`reserveFor`、`traitMods`／`traitText`、`sigilCooldown`／`sigilWarn`／`sigilGrowth`、`afterThunderWindow`、`enemyDamageScale`、`levelHpBonus`、`allyBaseScale`、`guardCounterScale`、`swordPuppetInherit`、`fireGuardShield`、`defendHP`／`defendLocked`／`bossHP`、`mechanismProfile`、`xpCostFor`／`growthNodeXP`、`loopScale`／`endlessAutoSkip`）；旧平衡1—3、成长1—4、遭遇1—7、挑战0／1的既有算式原样保留在旧分支，缺标记按1（挑战按0）恢复，成绩按规则分组。采纳值：阵盘450+60c、护身反击×0.8、符阵闪避2.4/留符4.5/留存1.8/成长1+.07L、雷法起手雷源+30/补雷20/雷威.40/节雷4/余电7秒、险境妖蛇×1.05、敌伤1+.10c、每8级+4生命、成长5经验表与.030、非符阵不抽震退、傀儡.90/御虫.85、百刃下限.10、镇阵+8盾、精密机括减速.15+伤害.05、初入仙途机关×0.5/开场4/首精英15秒、无尽×1.25/×1.18且第2圈起默认跳过已看。未采纳（开关保留但关闭）：本路每次保底、虫群回拉/集火优先、符阵起手减速、妖蛇8000、穿透+1、阵盘按难度/锁定一半、机关×0.75/开场6/无压力系数。等级派生状态（剑数与平衡4生命加成）统一由 `syncSwords()` 对账；快照校验先比「飞剑数量与等级」再比派生上限。B-11修持未实施；隔离实验候选不是新局默认；真人对照观察包待作者验证。
- 2026-09-14第二轮十玩家测试整改第三批（体验打磨，2026-09-15）：本批**不新增规则号、不改任何数值**（`BALANCE4`／`ENCOUNTER8`／`GROWTH5`／`CHALLENGE2`、`DIFFICULTIES`、修持、存档格式1、`fanren-qingzhu-v1`、records schema 1、`RULE_KEYS`、`GAME_VERSION`／`GAME_ARC` 全部原样）。提示分两条通道：战场中央 toast 战斗中 ≤1.2 秒且 `alpha≤0.6`（菜单/存档仍 3.5 秒），技能与战术确认（神雷施放、护身闪避反击含回盾数额）走 `cue` 通道不占单槽，噬甲满层按 `cueTimers['gnaw:'+id]` 每目标 30 秒一次且不序列化。紧凑 HUD 以 `--battle-header-base`（仅页眉底边）定位状态行，首领血条与目标提示按固定偏移排在其下，不得再用 `--battle-header-height` 自反馈；摇杆满偏半径取 `min(45*scale, 视觉半径)`；悟道卡整卡可点由拉伸既有 `.scene-action` 按钮实现（`::after{inset:0}`），保持一个键盘目标与一个可访问名，不新增激活路径。卡面「当前→修持后」与阶级、余电窗口、整备提示、雷源储量、休整基础值一律经 `traitMods`／`traitValueLine`／`afterThunderWindow`／`thunderCost`／`reserveRestBase` 读取，不得手抄。分步练习（`options.guidedPractice`）不再发满 12 项强化，使用正式 `dashCooldown`／`sigilCooldown`；练习新增表现与音效仍不消费战斗随机数、不发正式奖励。结算显示新增只读模块 `record-display.js`：固定计时下限按本圈 `receipts` 求和且秒数取自 `FIXED_NODE_SECONDS`／`treasureStepSeconds`／`illusionFirstWait`（不采用 `ENCOUNTERS.seconds` 旧元数据），差值 <100ms 显示「相同」，无尽另显 `chapterSplits` 求和的圈内用时；成绩分组键、records schema 与种子分组一律不动。经济4新建整备写入 `rest.cashOptions{gold:cash, skip:floor(cash/2), risk:cash*2}` 并由 `validatePhase3` 校验，缺字段的旧 rest 与经济1—3回落单一数额。账号级 `settings.skipSeenAfterWin` 默认关闭，命中时仍走 `advanceCinematic(true)`，不改 `cinematicSeen`／`cinematicHistory` 语义。幻境真门顺序 `objective.gateOrder` 只在 `extendedEncounter`（遭遇8＋平衡4）新局生成，取独立子域 `hashSeed(seed+':route-plan:v4:illusion:'+node)`，不消费 routePlan 生成器或战斗随机数；无 `gateOrder` 的旧局与已保存快照仍按 `(trueGate+1)%3`，`validatePacing` 拒绝排列非法或与 `done` 错位的快照。慢帧由 `frame-step.js` 拆为最多 4 个 ≤0.05 秒等长子步（单帧上限 0.2 秒），≤0.05 秒的帧与原单次 `update` 逐字节一致，子步不序列化。图鉴 `canon:false` 只标灵犀佩／炼晶；五行环／血色披风历史判定用取宝回执 `3:0`；**境界字样与玄骨死因的原著措辞未改写，待作者核对清单在 `qa-runs/2026-09-14-round2-three-batch/stepC/report.md`**。单手模式、成绩种子分组、B-11 修持与平衡调参不在本批；手机实机手感与提示可读性待作者真人验证。源码包白名单本批新增两个证据目录（排除 `.tar.gz`、`shots/`、`node_modules`）。
