# 1.0 发布验收报告

验收日期：2026-09-15。游戏：青竹剑阵 · 虚天殿篇 1.0。原创代码许可：GPL-3.0-only。

**结论：本地技术验收通过，源码、文档与离线交付已准备完成。** 公开仓库及 1.0 Release 入口见 [发布说明](publishing.md)。真实手机体验、九组原著措辞和外部参考图的授权来源保留明确核验边界。

## 本次完成

- 游戏显示改为 1.0，包版本 1.0.0；篇名、存档格式 1、存档键、成绩格式及全部玩法规则版本保持。
- 隔离原文满 12 份时先停止覆盖，不自动删除旧记录。清理前比较主档、备份、恢复日志与隔离原文；允许在异常恢复期间明确确认清理，再重新选择候选或导入。取消、存储删除失败与过时预览均有回归覆盖。
- 循息引流、轻墨遁符、雷后余势、合符同燃及图鉴／战况中的词条数值改为读取实际规则；图鉴注明当前新局基础规则，旧局按本局面板显示。没有改战斗数值。
- 重写玩家手册，当前数据表与源码函数同源；更新日志正文仅为“1.0正式发布”。历史资料完整留在本地，精选当前文档进入公开清单。
- 定向升级 React、vinext、Vite、Cloudflare 配套依赖；打包使用 fflate 0.8.3。项目内锁定 Playwright，移除作者机器的测试路径依赖。
- 补齐 GPLv3、11 项实际内嵌依赖的许可原文、700 项锁定依赖清单、素材与改编说明、贡献和问题模板、CI、构建与发布脚本。
- 本地 Git 仓库已初始化。源码 ZIP 由明确白名单生成并解压逐字节核验，HTML 与 ZIP 作为 Release 附件，不进入源码历史。

## 验收证据

汇总数据见 [验收 JSON](qa/acceptance.json)、[依赖审计](qa/audit.json)、[源码改动范围](qa/source-scope.json) 与 [开发页面冒烟](qa/dev-smoke.json)。完整原始日志、合成存档和截图保留在本地工作资料及 game/qa/release，不公开个人数据。

| 检查 | 结果 |
|---|---|
| 规则与存档自动测试 | 539 / 539，通过；0 失败、0 跳过 |
| lint | 0 新诊断；19 条原通用组件基线及哈希保持 |
| TypeScript | 通过；未将未启用 checkJs 的 JS 引擎称为完整类型认证 |
| 依赖审计 | 0 项问题，含开发依赖 |
| Node 兼容 | 22.13.1 与 24.15.0 检查、构建和打包通过 |
| Web 工程 | 构建通过；开发页面实际载入，标题 1.0、入口就绪、0 页面异常 |
| 双离线浏览器 | WebP／PNG 各五组套件；汇总 118 个检查组、36 条视口记录，0 页面异常；已监听的套件无外部请求 |
| 实际界面 | 桌面、360／390／420 竖屏及横屏；生命数字、正文、操作区、输入与许可帮助检查通过 |
| 声音 | WebAudio 实际生成与录制；总声音数上限 12，暂停／静音停止，峰值检查通过 |
| 资源 | 九张原 PNG 与编码哈希均核验；分别确认两份 HTML 嵌入了对应编码 |
| 打包 | 公开清单、链接、个人路径／凭据扫描、源码 ZIP 内容、交付哈希均通过 |
| 干净目录 | 从源码 ZIP 解压，重新 npm ci、检查、Web／离线构建与浏览器验证；重建 HTML 哈希与本地交付一致 |

全套浏览器检查后追加的图鉴／战况文案修订，在最终两份 HTML 与干净目录中又作针对性复验；118 是最终检查组覆盖汇总，不叠加重复运行冒充更多覆盖。

### 实战固定策略样本

真实引擎、六种起手、三档难度、共享种子 `release-v1.0`，空修持，保留全部胜败。无免伤、无额外经验，使用合法自动输入。

| 难度 | 局数 | 胜利 | 战败 | 存档校验 |
|---|---:|---:|---:|---:|
| 初入仙途 | 6 | 6 | 0 | 940 |
| 标准历练 | 6 | 6 | 0 | 946 |
| 险境求生 | 6 | 4 | 2 | 925 |
| 合计 | 18 | 16 | 2 | 2,811 |

全部正常结束，没有卡死、超时或存档校验失败。险境下雷法和符阵各有一局在第六章正常战败。样本只用于规则与稳定性检查，不代表真人胜率，也不证明各流派完全平衡。

### 回归重点

旧引擎与缺标记恢复、奖励凭据、成长与目标经验、无尽完成圈、观察模式隔离、覆盖确认与取消、故障重试、多窗口冲突、清场快照、机关与精英计时、真正松手及留符／挡招练习均有既有或新增回归。

本次浏览器脚本修正了过期夹具：旧合成成绩缺少新分段凭据、筛选栏已改为折叠、旧断言仍写成长4，以及 PNG 路径重复追加后缀。修正的是测试夹具或输入路径，没有放宽存档校验、删除失败断言或把游戏切回旧规则以求通过。

## 交付与复现

当前发布附件集中在 release/1.0/，包括两份 HTML、青竹剑阵_源码.zip、免安装玩家包、SHA256SUMS.txt 和 release-manifest.json；根目录两个 HTML 保留原游玩路径。源码包约 30 MiB，原约 350 MiB 的 beta 源码包已完整归档；公开包排除历史大证据和依赖。

WebP HTML 约 13.35 MiB，PNG HTML 约 24.38 MiB。最终精确大小与哈希以随附 manifest／SHA256SUMS 为准。源码包保留构建所需全部文件，最终验收报告与当前源码同包。

发布时按 [GitHub 发布步骤](publishing.md) 上传同次构建的成品与对应源码。本地与干净目录已执行对应验证流程；远程每次提交的实际结果见 [GitHub Actions](https://github.com/jindx1020-crypto/qingzhu-sword-array/actions/workflows/ci.yml)。

## 保留的边界

- 经济4旧局后续新整备采用灵石分档，已保存无 cashOptions 的整备保持旧金额；遭遇8＋平衡4旧局后续幻境用种子门序，已保存无 gateOrder 的幻境保持原序。用户已明确选择保留现状并如实披露。
- 不新增规则号、不重新调平衡；对照源码确认数值规则表、主引擎、路线和结算算法未变。
- 手机真机、单手手感、声音主观体验、提示阅读和持续无尽游玩意愿未完成真人验证。
- 九组境界／玄骨原著措辞尚未逐句核实，见 [改编说明](adaptation.md)。两张原始参考图的权利来源待作者确认，见 [素材说明](assets.md)；代码开源不代替图片或小说 IP 授权。
- 大体积内嵌图片会产生 Vite 的大 chunk 提示；本交付有意保持离线单文件。vinext 的路由静态分类提示不等于页面不可用，开发页面已实际验证。

## 免安装玩家包追加交付 · 2026-09-15

新增「青竹剑阵_1.0_免安装版.zip」，面向下载后直接游玩的玩家。解压后顶层「开始游戏.html」直接进入游戏；「兼容版／开始游戏_PNG.html」提供 PNG 图片兼容入口。包内附先读我、完整 GPLv3 许可和对应源码 ZIP，单独分享此包也保留对应源码。无需安装游戏、插件或开发环境。

入口 HTML 与本次常规／PNG 离线交付逐字节相同，嵌套源码 ZIP 与对应源包逐字节相同；没有增加启动器、后台服务或新的玩法规则。根游玩说明与游戏内帮助已同步入口名称，更新日志仍为“1.0正式发布”。

[玩家包验证记录](qa/player-package.json)：从实际 ZIP 解压，在断网 Chromium 的全新上下文中，通过普通界面直接开局（未启用调试 API），验证移动、闪避、暂停及刷新后的续玩记录。常规入口 1440×900 与 PNG 入口 390×844 均通过，0 页面异常、0 外部请求。此为浏览器验证，手机真机边界保持。

玩家包约 58 MiB（包含两个游戏入口和完整对应源码）；只需单文件的玩家也可单独下载约 13.35 MiB 的常规 HTML。最终精确大小与哈希见 release/1.0/ 内的 release-manifest.json／SHA256SUMS.txt。构建命令已自动生成玩家包，CI 已加入玩家包解压后验证与附件上传配置。
